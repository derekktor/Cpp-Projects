#include<iostream>
#include<string>

int main() {
    int login;
    std::string username;

    // Log in or Sign in
    std::cout << "Log in : 1\n";
    std::cout << "Sign in : 0\n";
    std::cin >> login;

    // If Log in -> username + password + password verification
    if (login == 1) {
        std::cout << "Please enter username:\n";
        std::cin >> username;
        if (username[0] == )
    }
    // If Sign in -> username + password
}